# RecallAR — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Tested with Python 3.10+

# Default backend — ArcFace ONNX via onnxruntime, works on macOS/Apple Silicon
insightface>=0.7.3
onnxruntime>=1.16.0

# Fallback backend — dlib-based, no TensorFlow
face_recognition>=1.3.0

# Image loading and processing
opencv-python==4.9.0.80

# Numerical operations (cosine similarity, statistics)
numpy==1.26.4

# Optional: DeepFace backend (may hang on macOS due to TensorFlow issues)
# Uncomment if you want to use --backend deepface
# deepface==0.0.93
# tf-keras>=2.16.0
